const path = require("path")

module.exports = {
    "config": path.resolve("src", "config", "postgres.js"),
    "models-path": path.resolve("src", "database", "model"),
    "migrations-path": path.resolve("src", "database", "migration"),
    "seeders-path": path.resolve("src", "database", "seeders")
}